<h1><%= t("app") %></h1>

<p><%= t("user_mailer.account_activation.greeting") %> <%= @user.name %>.</p>

<p>
  <%= t("user_mailer.account_activation.welcome") %>
  <%= t("user_mailer.account_activation.instruction") %>
</p>

<p><%= link_to t("user_mailer.account_activation.activate"), confirmation_url(@resource, confirmation_token: @token) %></p>
